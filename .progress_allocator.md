# allocator-fix progress

- Checkpoint 1 complete: reproduced the current failure, confirmed the existing global L2.0a oracle in `E:\opencell`, and recorded the exact `403 checked / 1022 unmapped` real-oracle baseline plus tick-0 shared-WID canary grants in `STATUS_ALLOCATOR_FIX.md`.
- Checkpoint 2 complete: global-oracle discovery is green in tests, so this worktree now sees the existing `E:\opencell` / `/mnt/e/opencell` L2.0a artifact instead of falsely reporting it missing.
- Checkpoint 3 complete: the allocator harness now uses the full 28-row oracle matrix with exact `build_wid_mappings` projection, fail-closed unmatched WIDs/rows, mc-key-preserving tick-0 canaries, AST-based composition guards, and explicit multi-tick skip behavior; focused pytest, `-rs` skip audit, Ruff, and the live L2.0a probe are green.
