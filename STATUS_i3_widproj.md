## STATUS_i3_widproj

- 2026-06-07T00:00:00Z Session start. Read `SESSION_CONTEXT.md` and i1 reference artifacts. Scope locked to RNADecay RNAs-primary duplicate-WID projection round-trip lossiness. Next: reproduce Beat 1 via `bin\oc-py REF_probe_widalign.py`, inspect overlay/projection/extractor contract, then commit each beat before proceeding.
- 2026-06-07T00:22:00Z Beat 1 reproduction complete. `bin\oc-py REF_probe_widalign.py` in the repo root currently fails bootstrap import (`_l2_2_design_a_runner_helpers` lives under `tests/vivarium` while the script computes `REPO_ROOT = parents[1]`), so I ran an equivalent path-stable probe through the same helper path via `bin\oc-py scripts/probe_i3_widproj_roundtrip.py`. Result matches i1/gate exactly: `mean_w1_round_trip_before_vs_after = 2.9466227347611205`; tick-0 raw RNA mean `0.8887973640856672` inflates to round-trip mean `5.046128500823723` with `234` mismatched indices; all-100-tick mean inflates from `0.8962067545304777` to `3.8424464579901154`. This confirms the FAIL is fully explained by overlay+projection round-trip lossiness, not by RNADecay compute.
- 2026-06-07T00:22:00Z Beat 1 overlay/projection contract documented. `_run_rna_decay_tick` overlays `oracle_before_rnas` into `state["rna"]["counts"]` via `overlay_observable_into_state(... observable="RNAs" ...)`, then exposes `oracle_after_rnas` as `trace_hint["RNAs_next"]`, then projects the measured OC result back out with `project_observable_from_state(... observable="RNAs" ...)`. Shared helper behavior is lossy for duplicate WIDs: `overlay_observable_into_state` writes `store[wids[idx]] = float(vector[idx])` into a dict store, so repeated RNA WIDs collapse to last-write-wins; `project_observable_from_state` then expands by the full duplicated 2428-long `rna_wids` list, replaying the collapsed value into every repeated slot. The extractor side shows the true Karr contract: `extract_per_process_traces_v2.m` snapshots the process-local `mod.RNAs` array positionally before/after `evolveState`, with no WID deduplication. Combined with the RNADecay/RNAProcessing docs and prior RNA-state notes (`State_Rna` is a 2428-entry full RNA state matrix, roughly form-state positions over mature species/TUs), the 2428-vector is a process-slot / transcript-position / RNA-form view, not a unique-WID dict view and not a molecule-instance-per-slot view. The harness therefore must preserve per-slot identity for duplicate-labeled WIDs.
- 2026-06-07T00:34:00Z Beat 2 design complete. Evaluated the three fix classes requested:
  - Fix-class A (chosen): add an RNADecay-only slot-preserving RNA shadow vector/store in the harness, feed that positional 2428-vector into RNADecay when present, and project RNAs back from that slot store rather than from the duplicate-collapsing dict. Also remove RNADecay's `trace_hint` overlay from the tick path so the harness measures honest SUT decay rather than the existing substrate-hint short-circuit. This preserves the exact Karr observable semantics (2428 positional slots), keeps scope inside RNADecay replay/harness code, and gives Beat-3's anti-laundering probe a real chance to pass.
  - Fix-class B (rejected): deduplicating to 378 unique WIDs would redefine the gate from "match Karr's 2428-slot RNAs observable" to "match a new aggregated observable". Even if both sides were deduped consistently, PASS would mean something different than it means today.
  - Fix-class C (rejected): reshaping `RnaDecayLightProcess` into a fully index-addressable 2428-array store would also preserve fidelity, but it is a larger SUT/interface rewrite than needed for this harness bug and risks collateral changes to non-L2.2 paths.
- 2026-06-07T00:34:00Z Beat 2 implementation sketch / invariant delta. Prior architecture guaranteed only unique-key RNA dict semantics in replay state; duplicate RNA slots were silently collapsed and RNADecay tick execution was short-circuited by `substrates_next` hints. Chosen change preserves existing Translation/Transcription/ProteinDecay helper behavior and the public gate observable (still 2428-slot RNAs vs Karr), but weakens the assumption that all replay observables can be represented only as WID-keyed dicts by adding an RNADecay-only positional shadow store. Deferred gap: if Translation `boundEnzymes` is the same duplicate-label class, carry that as i4 rather than widening i3 scope.
- 2026-06-07T01:02:00Z Beat 3 implementation complete. RNADecay tick path now seeds an RNADecay-only positional RNA shadow vector (`_l2_rna_slot_counts` / `_l2_rna_slot_wids`) alongside the legacy dict overlay, `RnaDecayLightProcess` reads that positional vector when present, exposes its per-slot RNA delta back to the harness, and `_run_rna_decay_tick` applies/project RNAs from the positional shadow store instead of from the duplicate-collapsing dict. RNADecay's L2.2 tick path no longer overlays `trace_hint` after-vectors, so the RNAs primary channel is measured from honest SUT compute rather than hint replay / substrate short-circuit.
- 2026-06-07T01:02:00Z Beat 3 verification complete. `bin\oc-py scripts/probe_i3_widproj_roundtrip.py` now reports `tick0.max_abs_diff = 0.0`, `tick0.mean_round_trip = 0.8887973640856672`, `tick0.mismatch_count = 0`, and `mean_w1_round_trip_before_vs_after = 0.0003830313014827025` (equal to raw before-vs-after). Anti-laundering probe `bin\oc-py scripts/probe_i3_rna_decay_anticheat.py` reports `cheated_is_all_zero = false`, `outputs_match_exactly = true`, `max_abs_diff_honest_vs_cheated = 0.0`. Pytest verification: mandated anticheat slice `tests/vivarium/test_l2_2_design_a_runner_anticheat.py tests/vivarium/test_l2_2_design_a_runner_protein_decay_anticheat.py tests/vivarium/test_l2_2_design_a_runner_anticheat_rna_decay.py -q` = `13 passed`; additional RNADecay slice `tests/vivarium/test_karr_rna_decay.py tests/vivarium/test_karr_rna_decay_l2_replay.py -q` = `5 passed`.
- 2026-06-07T01:18:00Z Beat 4 smoke gate complete. Ran `bin\oc-py -m tests.vivarium.l2_2_design_a_runner --process RNADecay --ticks 10 --seeds 5 --bootstrap 200 --output-dir tests/vivarium/artifacts/l2_2_design_a/RNADecay_i3_smoke`. Outcome: process verdict still `FAIL` because `substrates` remains `FAIL@62.691282051282066`, but the target RNAs primary channel is now `PASS@0.0007578253706754489` with 95% CI `[0.0006670098846787421, 0.0008484349258649071]`. Verdict: i3 fix restored the RNAs signal (`0.000758 << 1.0`, and massively below the prior `2.9466` failure); substrate divergence is a separate out-of-scope issue for this task.
- 2026-06-07T01:22:00Z Beat 5 spot-check complete (read-only). Ran `bin\oc-py scripts/probe_i3_translation_bound_enzymes_wids.py` against `runner_helpers._translation_process(0)`. Translation `boundEnzymes` uses `enzyme_wids`, and that list is `16 total / 16 unique / 0 duplicate WIDs`. Verdict: no duplicate-WID evidence here; do not open an i4 from this specific `boundEnzymes` accessor.
