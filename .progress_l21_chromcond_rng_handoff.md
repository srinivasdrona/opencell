# Progress: L21 ChromosomeCondensation RNG Handoff

- 2026-08-17 18:53 IST: Loaded `SESSION_CONTEXT.md`, all prior ChromCond status files, `STATUS_L21_CHROMCOND_GEOMETRY_FIX.md`, `PROMPT_RNG_HANDOFF_FIX.md`, the primary WholeCell source for `Process`, `Simulation.initializeState`, `Simulation.evolveState`, and the replay/fix templates.
- 2026-08-17 18:53 IST: Confirmed the direct `initializeState()` path does not itself explain a post-warmup process RNG reset; `copyFromState()` and `copyToState()` never touch `randStream`.
- 2026-08-17 18:53 IST: Traced the actual extraction boundary in primary source/helpers: `karr_bootstrap()` loads `Simulation_fitted.mat`, then `seed_simulation(sim, seed)` calls `Simulation.seedRandStream()`, which reseeds the simulation, all states, and all processes to the seed-default stream state while preserving the already loaded chromosome and molecule surface.
- 2026-08-17 18:53 IST: Next step is to remove the replay-time restoration of the warmup endpoint RNG (`1279689633`) from production while preserving the loaded state surface, then rerun the hidden tick-0 comparison, no-hint replay, strict rubric, focused tests, L1b anchors, and Ruff.
- 2026-08-17 19:05 IST: Verified production now starts replay from `MatlabRandStream(seed, generator="mcg16807")` and no longer restores the warmup endpoint RNG, while still restoring the validated post-warmup SMC pool state.
- 2026-08-17 19:05 IST: Re-ran the focused checks. `bin/oc-pytest.cmd tests/vivarium/test_karr_chromosome_condensation.py -q` -> `7 passed`; `bin/oc-pytest.cmd tests/vivarium/test_karr_chromosome_condensation_l2_replay.py -q` -> `1 passed`; `bin/oc-py.cmd tmp/chromcond_nohint_probe.py` -> `NO_MISMATCH`; `bin/oc-py.cmd -m ruff check opencell/vivarium/karr_chromosome_condensation.py tests/vivarium/test_karr_chromosome_condensation.py` -> PASS.
- 2026-08-17 19:05 IST: The first hidden mismatch moved off tick 0. `bin/oc-py.cmd tmp/chromcond_hidden_mismatch_probe.py` now reports tick `1`, `karr_len 206`, `oc_len 0`, `extra []`, with missing examples `(24, 3, 50)`, `(2553, 1, 200)`, `(3342, 0, 82)`, `(7911, 0, 1)`, `(9947, 0, 82)`, `(18502, 0, 82)`.
- 2026-08-17 19:05 IST: `bin/oc-py.cmd scripts/probe_l2_1_strict_rubric.py --process ChromosomeCondensation` remains `FAIL` with `Karr 66%`, `OC 71%`, `OC|Karr 95%`. `bin/oc-py.cmd scripts/l1b_verify_wiring.py --process ChromosomeCondensation --strict-anchors --format plain` still fails only `check_oc_anchors_resolve`, matching the pre-existing dirty `data/schemas/per_process_wiring/ChromosomeCondensation.yaml`.
- 2026-08-17 19:05 IST: Acceptance threshold for this task is met by the exact next post-handoff divergence: replay tick 0 now uses the same seeded process RNG boundary as live WholeCell extraction, and the next bug is a separate hidden chromosome carryover/application mismatch between tick 0 and tick 1.
