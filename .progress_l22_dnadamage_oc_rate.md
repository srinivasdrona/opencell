# Progress: L22 DNADamage OC Rate

- 2026-08-18: resumed from partial reaction-law patch set and verified the core structural change is in place in `opencell/vivarium/karr_dna_damage.py`.
- Green chunk 1:
  - fixed the focused vivarium reaction-law tests so they assert the real per-reaction lambda path and use sequence-sampled UV fixtures where sampler semantics are under test
  - fixed `scripts/matlab/karr_bootstrap.m` fail-closed overlay verification to compare resolved DNADamage content on the same normalized-text hash basis as the source/patched hashes
  - confirmed provenance plumb-through remains present in `scripts/matlab/extract_per_process_traces_v2.m`
  - verification:
    - `bin\oc-pytest.cmd tests/vivarium/test_karr_dna_damage.py -vv --maxfail=1`
    - `bin\oc-pytest.cmd tests/scripts/test_extract_per_process_traces_v2_static.py -q`
    - `bin\oc-pytest.cmd tests/scripts/test_karr_bootstrap_mnrnd_provider_smoke.py -q`
- Next:
  - promote the reusable verifier into a tracked script/evidence path
  - rerun the fixed OC side against the existing genuine N50 Karr corpus
  - write final `STATUS_L22_DNADAMAGE_OC_RATE.md` with the mechanical verdict
