# L22 DNADamage Genuine Progress

## Slot 1 — Deliberate Action Prefix

1. Resolve the exact Karr-side target-process contradiction before any new cohort extraction.
2. Port only source-backed event infra fixes and reusable target-only diagnostics from `wave-l22-dnadamage`.
3. Require a genuine-provider 5-seed canary with nonzero Karr support near the preregistered expectation before any N=50 rerun.
4. Preserve honest failure states; do not reuse the old shim-bound/event-vacuous UVB cohort as evidence.

## Slot 2 — Composition / Guardrails

- Python execution must use the WSL wrappers in `bin\`.
- No threshold/catalog/index changes.
- No synthetic evidence and no copying the old 50-trace UVB cohort.
- Commit each green chunk before moving to the next.

## Slot 3 — Current Findings

- `SESSION_CONTEXT.md`, the event window extractor contract, the genuine-provider contract, the wave-branch DNADamage statuses, the current stimulus cohort planner/verifier, and primary `DNADamage.m` / `Process.m` / `Chromosome.m` source paths have been read.
- Legacy reusable commits identified:
  - `fdd1cc8` preserves nested DNADamage stimulus cohort output roots in `scripts/l2_event/launcher.py`.
  - `3077753` teaches `scripts/l2_event/window_loader.py` to materialize chromosome group-ref payloads safely.
- The legacy branch already isolated the first Karr-side contradiction inside `DNADamage.m::evolveState()` on the target-only scheduler surface:
  - UVB rate support is live (`uvb_rate_sum_pre_evolve = 0.1`).
  - accessible UVB motifs exist and direct `setSiteDamaged()` can mutate them.
  - yet the loop guard fires first because `maxReactions` becomes `-Inf`.
- Recorded legacy artifacts rule out the simplistic "negative substrates" explanation and do not yet prove a sparse/full matrix fix:
  - `negative_substrates_probe.json` reports no negative substrate slots.
  - `representation_fix_probe.json` reports `issparse_reaction_small_molecule_matrix = false` even before the attempted `full(...)` normalization.

## Next

1. Inspect the exact denominator/ratio sign behavior that makes `maxReactions` serialize as `-Inf` despite positive UVB support.
2. Patch only the proven representation/allocation defect, then verify with a fresh genuine-provider canary.
3. Scale to 50 only if the canary shows nonzero Karr support near the preregistered expectation.

## 2026-08-18 Update

- Ported the two source-backed event infra fixes into this worktree:
  - nested condition-root output-subdir preservation in `scripts/l2_event/launcher.py`
  - chromosome group-ref materialization in `scripts/l2_event/window_loader.py`
- Added reusable target-only MATLAB diagnostics:
  - `scripts/matlab/l22_dnadamage_global_override_probe.m`
  - `scripts/matlab/l22_dnadamage_uvb_sampling_probe.m`
  - `scripts/matlab/l22_dnadamage_evolvestate_inline_probe.m`
- WSL-backed focused verification is green:
  - `bin\oc-pytest.cmd tests/scripts/test_l2_event_launcher.py -q` -> `84 passed`
  - `bin\oc-pytest.cmd tests/scripts/test_l2_event_window_loader.py -q` -> `26 passed, 4 skipped`
- Live MATLAB repro on this branch is now confirmed:
  - `scripts/matlab/l22_dnadamage_evolvestate_inline_probe.m` wrote `artifacts/l22_dnadamage_genuine/evolvestate_inline_probe_tick1.json`
  - the artifact reproduces the legacy contradiction exactly: `uvb_rate_sum_pre_evolve = 0.1`, `total_rate_sum_pre_evolve = 0.10001982309999105`, and every UVB reaction guard-skips with `max_reactions_raw_text = "-Inf"` before `setSiteDamaged()`.
