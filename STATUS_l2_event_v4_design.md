# L2 Event v4 Design Status

## Beat 1 - DESIGN_TEMPLATE + v0.3 baseline read

Five-line summary:
1. `DESIGN_TEMPLATE.md` requires a fresh design doc with slot-1 carryover, §1-§11 in fixed order, and an explicit acceptance-bar self-audit; review readiness is a property of structure plus falsifiability, not prose completeness.
2. The template's hard disciplines are the inventory manifest, cross-surface map, decision cards with Beat-4 inversions, falsifiable claims, open operator questions, scope boundary, migration path, risks, and operator checklist.
3. v0.3 material that survives conceptually: the problem statement, final in-scope set (`Cytokinesis`, `RibosomeAssembly`), the need for Karr-state-conditioned one-tick OC probes, deterministic seeding, and rejection of p-value-as-equivalence logic.
4. v0.3 material that needs fundamental rework: firing-tick-only versus window-grid contradictions, sparse-grid/inter-arrival treatment for RibosomeAssembly, bootstrap design, Cytokinesis verdict semantics under redundant magnitude, and incomplete production contract/provenance language.
5. Additional reality check from source inspection: the canonical Karr `.m` paths are referenced by local schemas/extract docs but are not populated in this checkout, and the target event traces for `Cytokinesis`/`RibosomeAssembly` do not yet exist, so v4 must treat extraction as a first-class migration step rather than an assumed prerequisite.

## Beat 2 - New DESIGN_TEMPLATE doc

Pending.

## Beat 3 - Acceptance-bar self-rubber-duck

Pending.

## Beat 4 - Inversion

Pending.
