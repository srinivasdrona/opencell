[
  {
    "file": "extract_cell_cycle_trajectory.m",
    "signature": "(script)",
    "save_calls": "save(out_path, 'snapshots', 'metadata', '-v7.3');",
    "output_lines": "out_path = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', 'cell_cycle_trajectory.mat'); || if exist(out_path, 'file') || fprintf('[cell_cycle] output already exists: %s\\n', out_path); || save(out_path, 'snapshots', 'metadata', '-v7.3'); || fprintf('[cell_cycle] saved: %s\\n', out_path);",
    "seed_lines": "'rng_seed', uint32(0), ...",
    "tick_lines": "n_total_steps = 32400;  % ~9 hours at dt=1s; Karr's published cell cycle length || fprintf('[cell_cycle] starting run: %d ticks at dt=%g s\\n', n_total_steps, sim.stepSizeSec); || for tick_idx = 1:n_total_steps || tick_idx, n_total_steps, 100*tick_idx/n_total_steps, elapsed, ... || elapsed * (n_total_steps - tick_idx) / tick_idx); || 'n_total_steps_target', n_total_steps, ..."
  },
  {
    "file": "extract_fitted_constants.m",
    "signature": "(script)",
    "save_calls": "save(out_path, 'fitted', 'metadata', '-v7.3');",
    "output_lines": "out_path = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', 'fitted_constants.mat'); || if exist(out_path, 'file') || fprintf('[fit] output already exists: %s\\n', out_path); || save(out_path, 'fitted', 'metadata', '-v7.3'); || fprintf('\\n[fit] saved: %s\\n', out_path);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_initial_states.m",
    "signature": "(script)",
    "save_calls": "save(out_path, 'init_state', 'metadata', '-v7.3');",
    "output_lines": "out_root = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', 'initial_states'); || if ~exist(out_root, 'dir') || mkdir(out_root); || out_path = fullfile(out_root, [pname '_init.mat']); || if exist(out_path, 'file') || save(out_path, 'init_state', 'metadata', '-v7.3');",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_karr_m1_dynamics.m",
    "signature": "function extract_karr_m1_dynamics(wholecellRoot, outDir)",
    "save_calls": "save(outFile, '-struct', 'out', '-v7.3');",
    "output_lines": "function extract_karr_m1_dynamics(wholecellRoot, outDir) || if nargin < 2, outDir = fullfile(wholecellRoot,'karr_flat'); end || if ~exist(outDir,'dir'), mkdir(outDir); end || outFile = fullfile(outDir, 'metabolism_dynamics.mat'); || save(outFile, '-struct', 'out', '-v7.3'); || fprintf('\\nSaved %s\\n', outFile);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_karr_m1_flux_growth.m",
    "signature": "function extract_karr_m1_flux_growth(wholecellRoot, outDir)",
    "save_calls": "save(outFile, '-struct', 'out', '-v7.3');",
    "output_lines": "function extract_karr_m1_flux_growth(wholecellRoot, outDir) || if nargin < 2, outDir = fullfile(wholecellRoot, 'karr_flux_extract'); end || if ~exist(outDir, 'dir'), mkdir(outDir); end || outFile = fullfile(outDir, 'metabolism_matlab_flux_growth.mat'); || save(outFile, '-struct', 'out', '-v7.3'); || fprintf('\\nSaved %s\\n', outFile);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_karr_m2v2.m",
    "signature": "function extract_karr_m2v2(wholecellRoot, outDir)",
    "save_calls": "save(outFile, 'data', '-v7');",
    "output_lines": "function extract_karr_m2v2(wholecellRoot, outDir) || if nargin < 2, outDir = fullfile(wholecellRoot,'karr_flat'); end || if ~exist(outDir,'dir'), mkdir(outDir); end || outFile = fullfile(outDir, 'transcription_v2_targeted.mat'); || save(outFile, 'data', '-v7'); || fprintf('\\n[OK] wrote %s\\n', outFile);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_karr_m3v2.m",
    "signature": "function extract_karr_m3v2(wholecellRoot, outDir)",
    "save_calls": "save(outFile, 'data', '-v7');",
    "output_lines": "function extract_karr_m3v2(wholecellRoot, outDir) || if nargin < 2, outDir = fullfile(wholecellRoot,'karr_flat'); end || if ~exist(outDir,'dir'), mkdir(outDir); end || outFile = fullfile(outDir, 'translation_v2_targeted.mat'); || save(outFile, 'data', '-v7'); || fprintf('\\n[OK] wrote %s\\n', outFile);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_karr_mats.m",
    "signature": "function extract_karr_mats(wholecellRoot, outDir)",
    "save_calls": "save(outPath, 'data', '-v7'); %#ok<NASGU>",
    "output_lines": "function extract_karr_mats(wholecellRoot, outDir) || %   outDir        : destination folder for *_flat.mat and manifest.json. || if nargin < 2 || isempty(outDir), outDir = fullfile(wholecellRoot, 'karr_flat'); end || if ~exist(outDir, 'dir'), mkdir(outDir); end || fprintf('[extract_karr_mats] Output dir    : %s\\n', outDir); || outPath = fullfile(outDir, outName); || save(outPath, 'data', '-v7'); %#ok<NASGU> || entry.output_sha256 = sha256OfFile(outPath); || entry.output_bytes  = fileSize(outPath); || manifestPath = fullfile(outDir, 'manifest.json'); || fid = fopen(manifestPath, 'w'); || fprintf('\\n[extract_karr_mats] Manifest written: %s\\n', manifestPath);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_karr_targeted.m",
    "signature": "function extract_karr_targeted(wholecellRoot, outDir)",
    "save_calls": "save(flatMat, 'data', '-v7'); || save(kbMat,'data','-v7'); || save(protMat,'data','-v7'); || save(rnaMat,'data','-v7');",
    "output_lines": "function extract_karr_targeted(wholecellRoot, outDir) || if nargin < 2, outDir = fullfile(wholecellRoot,'karr_flat'); end || if ~exist(outDir,'dir'), mkdir(outDir); end || flatMat = fullfile(outDir, 'sim_fitted_targeted.mat'); || kbMat = fullfile(outDir,'knowledgeBase_targeted.mat'); || protMat = fullfile(outDir,'proteins_targeted.mat'); || extract_rna_state(sim, outDir); || function out = extract_rna_state(sim, outDir) || rnaMat = fullfile(outDir,'rnas_targeted.mat');",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_m3_metabolite_vocab.m",
    "signature": "function extract_m3_metabolite_vocab()",
    "save_calls": "save(out_path, 'metabolite_722_ids', '-v7.3');",
    "output_lines": "out_path = 'data/m1_sources/karr_flat/m3_metabolite_vocab.mat'; || save(out_path, 'metabolite_722_ids', '-v7.3'); || fprintf('Wrote %s\\n', out_path);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_metab_flux_per_tick.m",
    "signature": "function extract_metab_flux_per_tick(wholecellRoot, outDir, traceRoot, seedFirst, seedLast, tickFirst, tickLast)",
    "save_calls": "save(outFile, '-struct', 'out', '-v7.3');",
    "output_lines": "function extract_metab_flux_per_tick(wholecellRoot, outDir, traceRoot, seedFirst, seedLast, tickFirst, tickLast) || %   outDir        : output directory for per-tick .mat files || %   traceRoot     : path to per_process_traces_v2_s{NNN}/ tree || % Output: outDir/metab_flux_per_tick_s{NNN}_t{TT}.mat for each (seed, tick) || if ~exist(outDir, 'dir'), mkdir(outDir); end || traceFile = fullfile(traceRoot, sprintf('per_process_traces_v2_s%03d', seedVal), ... || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_t%03d.mat', seedVal, t)); || if exist(outFile, 'file') || save(outFile, '-struct', 'out', '-v7.3');",
    "seed_lines": "function extract_metab_flux_per_tick(wholecellRoot, outDir, traceRoot, seedFirst, seedLast, tickFirst, tickLast) || % and running calcFluxBounds + calcGrowthRate. Saves one .mat per (seed, tick) || %   seedFirst     : first seed to extract (uint32) || %   seedLast      : last seed to extract (uint32, inclusive) || % Output: outDir/metab_flux_per_tick_s{NNN}_t{TT}.mat for each (seed, tick) || for seedVal = uint32(seedFirst:seedLast) || traceFile = fullfile(traceRoot, sprintf('per_process_traces_v2_s%03d', seedVal), ... || fprintf('SKIP seed=%d: trace file not found: %s\\n', seedVal, traceFile); || fprintf('\\n=== seed=%d: loading trace %s ===\\n', seedVal, traceFile); || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_t%03d.mat', seedVal, t)); || error('Unexpected pre_sub shape at seed=%d t=%d: [%d %d]', ... || seedVal, t, size(pre_sub, 1), size(pre_sub, 2)); || out.x_seed = seedVal; || fprintf('  seed=%d t=%03d: growth=%.3e flux_L1=%.3e\\n', ... || seedVal, t, growth_t, sum(abs(flux_t))); || fprintf('  seed=%d: done.\\n', seedVal);",
    "tick_lines": "function extract_metab_flux_per_tick(wholecellRoot, outDir, traceRoot, seedFirst, seedLast, tickFirst, tickLast) || %   tickFirst     : first tick index (1-based, matching Karr trace indexing) || %   tickLast      : last tick index (inclusive) || if nargin < 6, tickFirst = 1; end || if nargin < 7, tickLast = 100; end || n_ticks_in_trace = numel(sub_cells); || n_ticks_in_trace, tickFirst, min(tickLast, n_ticks_in_trace)); || for t = tickFirst:min(tickLast, n_ticks_in_trace) || if mod(t, 10) == 0 || t == tickFirst"
  },
  {
    "file": "extract_metab_flux_v3.m",
    "signature": "function extract_metab_flux_v3(wholecellRoot, outDir, seed)",
    "save_calls": "save(outFile, '-struct', 'out', '-v7.3');",
    "output_lines": "function extract_metab_flux_v3(wholecellRoot, outDir, seed) || if nargin < 2, outDir = fullfile(wholecellRoot, 'metab_flux_extract'); end || if ~exist(outDir, 'dir'), mkdir(outDir); end || outFile = fullfile(outDir, sprintf('metab_flux_allocated_state_s%03d_tick1.mat', seed)); || save(outFile, '-struct', 'out', '-v7.3'); || fprintf('\\nSaved %s\\n', outFile);",
    "seed_lines": "function extract_metab_flux_v3(wholecellRoot, outDir, seed) || if nargin < 3, seed = uint32(0); end || fprintf('Target: Metabolism (proc_idx=%d), seed=%d\\n', target_idx, seed); || sim.applyOptions('seed', seed); || sim.seedRandStream(); || if isprop(sim.processes{i}, 'seed') || sim.processes{i}.seed = seed; || if ismethod(sim.processes{i}, 'seedRandStream') || sim.processes{i}.seedRandStream(); || out.x_seed = seed; || outFile = fullfile(outDir, sprintf('metab_flux_allocated_state_s%03d_tick1.mat', seed));",
    "tick_lines": ""
  },
  {
    "file": "extract_metab_flux_with_allocation_v2.m",
    "signature": "function extract_metab_flux_with_allocation_v2(wholecellRoot, outDir, seed, nTicks)",
    "save_calls": "save(outFile, '-struct', 'out', '-v7.3');",
    "output_lines": "function extract_metab_flux_with_allocation_v2(wholecellRoot, outDir, seed, nTicks) || if nargin < 2, outDir = fullfile(wholecellRoot, 'metab_flux_extract'); end || if ~exist(outDir, 'dir'), mkdir(outDir); end || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_%dticks.mat', seed, nTicks)); || save(outFile, '-struct', 'out', '-v7.3'); || fprintf('\\nSaved %s\\n', outFile);",
    "seed_lines": "function extract_metab_flux_with_allocation_v2(wholecellRoot, outDir, seed, nTicks) || if nargin < 3, seed = uint32(0); end || fprintf('Target: Metabolism (proc_idx=%d), seed=%d, n_ticks=%d\\n', target_idx, seed, nTicks); || % Seed simulation (mirror seed_simulation in v2 script) || sim.applyOptions('seed', seed); || sim.seedRandStream(); || if isprop(sim.processes{i}, 'seed') || sim.processes{i}.seed = seed; || if ismethod(sim.processes{i}, 'seedRandStream') || sim.processes{i}.seedRandStream(); || out.x_seed = seed; || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_%dticks.mat', seed, nTicks));",
    "tick_lines": "function extract_metab_flux_with_allocation_v2(wholecellRoot, outDir, seed, nTicks) || if nargin < 4, nTicks = uint32(10); end || fprintf('Target: Metabolism (proc_idx=%d), seed=%d, n_ticks=%d\\n', target_idx, seed, nTicks); || flux_records = cell(nTicks, 1); || growth_records = zeros(nTicks, 1); || bounds_records = cell(nTicks, 1); || pre_substrates = cell(nTicks, 1); || pre_enzymes = cell(nTicks, 1); || post_substrates = cell(nTicks, 1); || for t = 1:nTicks || out.x_n_ticks = nTicks; || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_%dticks.mat', seed, nTicks));"
  },
  {
    "file": "extract_metab_flux_with_allocation.m",
    "signature": "function extract_metab_flux_with_allocation(wholecellRoot, outDir, seed, nTicks)",
    "save_calls": "save(outFile, '-struct', 'out', '-v7.3');",
    "output_lines": "function extract_metab_flux_with_allocation(wholecellRoot, outDir, seed, nTicks) || if nargin < 2, outDir = fullfile(wholecellRoot, 'metab_flux_extract'); end || if ~exist(outDir, 'dir'), mkdir(outDir); end || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_%dticks.mat', seed, nTicks)); || save(outFile, '-struct', 'out', '-v7.3'); || fprintf('\\nSaved %s\\n', outFile);",
    "seed_lines": "function extract_metab_flux_with_allocation(wholecellRoot, outDir, seed, nTicks) || if nargin < 3, seed = uint32(0); end || fprintf('Target: Metabolism (proc_idx=%d), seed=%d, n_ticks=%d\\n', target_idx, seed, nTicks); || % Seed the simulation — mirror seed_simulation from extract_per_process_traces_v2.m || sim.applyOptions('seed', seed); || sim.seedRandStream(); || if isprop(sim.processes{i}, 'seed') || sim.processes{i}.seed = seed; || if ismethod(sim.processes{i}, 'seedRandStream') || sim.processes{i}.seedRandStream(); || out.x_seed = seed; || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_%dticks.mat', seed, nTicks));",
    "tick_lines": "function extract_metab_flux_with_allocation(wholecellRoot, outDir, seed, nTicks) || if nargin < 4, nTicks = uint32(10); end || fprintf('Target: Metabolism (proc_idx=%d), seed=%d, n_ticks=%d\\n', target_idx, seed, nTicks); || flux_records = cell(nTicks, 1); || growth_records = zeros(nTicks, 1); || bounds_records = cell(nTicks, 1); || pre_substrates = cell(nTicks, 1); || pre_enzymes = cell(nTicks, 1); || post_substrates = cell(nTicks, 1); || for t = 1:nTicks || out.x_n_ticks = nTicks; || outFile = fullfile(outDir, sprintf('metab_flux_per_tick_s%03d_%dticks.mat', seed, nTicks));"
  },
  {
    "file": "extract_per_process_fixtures.m",
    "signature": "function extract_per_process_fixtures(wholecellRoot, outDir)",
    "save_calls": "save(outPath, 'data', '-v7');",
    "output_lines": "function extract_per_process_fixtures(wholecellRoot, outDir) || %   outDir        : destination folder. Default: || %   <outDir>/<Name>_flat.mat   - v7 .mat with one struct 'data' holding the || %   <outDir>/matlab_extract_manifest.json - MATLAB release, sha256 per || if nargin < 2 || isempty(outDir) || outDir = fullfile('data', 'karr_fixtures', 'per_process'); || if ~exist(outDir, 'dir'), mkdir(outDir); end || fprintf('[per_process] Output dir    : %s\\n', outDir); || outPath = fullfile(outDir, outName); || save(outPath, 'data', '-v7'); || entry.output_sha256 = sha256OfFile(outPath); || entry.output_bytes  = fileSize(outPath); || manifestPath = fullfile(outDir, 'matlab_extract_manifest.json'); || fid = fopen(manifestPath, 'w'); || fprintf('\\n[per_process] Manifest: %s\\n', manifestPath);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_per_process_traces_batch_a.m",
    "signature": "(script)",
    "save_calls": "",
    "output_lines": "",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_per_process_traces_batch_b.m",
    "signature": "(script)",
    "save_calls": "",
    "output_lines": "",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_per_process_traces_batch_c.m",
    "signature": "(script)",
    "save_calls": "",
    "output_lines": "",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_per_process_traces_batch_d.m",
    "signature": "(script)",
    "save_calls": "",
    "output_lines": "",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_per_process_traces_fix.m",
    "signature": "function extract_per_process_traces_fix(n_ticks)",
    "save_calls": "save(out_path, 'states_before', 'states_after', 'metadata', '-v7');",
    "output_lines": "out_root = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', 'per_process_traces'); || if ~exist(out_root, 'dir') || mkdir(out_root); || fprintf('[trace_fix] output dir: %s\\n', out_root); || out_path = fullfile(out_root, sprintf('%s_%dticks.mat', pname, n_ticks)); || fprintf('[trace_fix] skipped write (keeping existing file untouched): %s\\n', out_path); || save(out_path, 'states_before', 'states_after', 'metadata', '-v7'); || fprintf('[trace_fix] wrote: %s\\n', out_path);",
    "seed_lines": "seed_simulation(sim, uint32(0)); || 'rng_seed', uint32(0), ... || function seed_simulation(sim, seed) || if isobject(sim) && ismethod(sim, 'applyOptions') && ismethod(sim, 'seedRandStream') || sim.applyOptions('seed', seed); || sim.seedRandStream(); || sim.randStream.seed = seed;",
    "tick_lines": "function extract_per_process_traces_fix(n_ticks) || if nargin < 1 || isempty(n_ticks) || n_ticks = 100; || out_path = fullfile(out_root, sprintf('%s_%dticks.mat', pname, n_ticks)); || states_before.(snapshot_props{p}) = cell(n_ticks, 1); || states_after.(snapshot_props{p}) = cell(n_ticks, 1); || for t = 1:n_ticks || 'n_ticks', n_ticks, ..."
  },
  {
    "file": "extract_per_process_traces_v2.m",
    "signature": "function extract_per_process_traces_v2(process_names, output_subdir, n_ticks, seed)",
    "save_calls": "save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3');",
    "output_lines": "out_root = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', output_subdir); || if ~exist(out_root, 'dir') || mkdir(out_root); || out_path = fullfile(out_root, sprintf('%s_%dticks.mat', canonical_name, n_ticks)); || if exist(out_path, 'file') || fprintf('[trace_v2] already exists, skipping: %s\\n', out_path); || fprintf('[trace_v2] skipped write: %s\\n', out_path); || save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3'); || fprintf('[trace_v2] saved: %s\\n', out_path);",
    "seed_lines": "function extract_per_process_traces_v2(process_names, output_subdir, n_ticks, seed) || if nargin < 4 || isempty(seed) || seed = uint32(0); || seed = uint32(seed); || if seed == 0 || output_subdir = sprintf('per_process_traces_v2_s%03d', seed); || seed_simulation(sim, seed); || 'rng_seed', seed, ... || function seed_simulation(sim, seed) || if isobject(sim) && ismethod(sim, 'applyOptions') && ismethod(sim, 'seedRandStream') || sim.applyOptions('seed', seed); || sim.seedRandStream(); || sim.randStream.seed = seed;",
    "tick_lines": "function extract_per_process_traces_v2(process_names, output_subdir, n_ticks, seed) || %   data/m1_sources/karr_native/<output_subdir>/<Process>_<n_ticks>ticks.mat || if nargin < 3 || isempty(n_ticks) || n_ticks = 100; || out_path = fullfile(out_root, sprintf('%s_%dticks.mat', canonical_name, n_ticks)); || states_before.(snapshot_props{p}) = cell(n_ticks, 1); || states_after.(snapshot_props{p}) = cell(n_ticks, 1); || for t = 1:n_ticks || 'n_ticks', n_ticks, ..."
  },
  {
    "file": "extract_per_process_traces.m",
    "signature": "function extract_per_process_traces(process_names, output_subdir, n_ticks)",
    "save_calls": "save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3');",
    "output_lines": "out_root = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', output_subdir); || if ~exist(out_root, 'dir') || mkdir(out_root); || out_path = fullfile(out_root, [pname '_' num2str(n_ticks) 'ticks.mat']); || if exist(out_path, 'file') || fprintf('[trace] already exists, skipping: %s\\n', out_path); || save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3'); || fprintf('[trace] saved: %s\\n', out_path);",
    "seed_lines": "%   metadata               struct: n_ticks, rng_seed, process_name, timestamp || % Seed RNG deterministically — Simulation class API varies across WCM versions || if ismethod(sim, 'applyOptions') && ismethod(sim, 'seedRandStream') || sim.applyOptions('seed', uint32(0)); || sim.seedRandStream(); || sim.randStream.seed = uint32(0); || catch seed_err || fprintf('[trace] WARN could not seed RNG for %s: %s\\n', pname, seed_err.message); || 'rng_seed', uint32(0), ...",
    "tick_lines": "function extract_per_process_traces(process_names, output_subdir, n_ticks) || % For each process named in PROCESS_NAMES, freeze inputs, run N_TICKS of || %   n_ticks: how many ticks to run per process (default 100) || %   states_before          struct of per-property snapshots, each (n_ticks, ...) || %   metadata               struct: n_ticks, rng_seed, process_name, timestamp || if nargin < 3 || isempty(n_ticks) || n_ticks = 100; || out_path = fullfile(out_root, [pname '_' num2str(n_ticks) 'ticks.mat']); || % Run n_ticks of evolveState on this process || for t = 1:n_ticks || 'n_ticks', n_ticks, ..."
  },
  {
    "file": "extract_protein_complexes.m",
    "signature": "function extract_protein_complexes(wholecellRoot, outDir)",
    "save_calls": "save(outFile, 'data', '-v7');",
    "output_lines": "function extract_protein_complexes(wholecellRoot, outDir) || if nargin < 2, outDir = fullfile(wholecellRoot, 'karr_flat'); end || if ~exist(outDir, 'dir'), mkdir(outDir); end || outFile = fullfile(outDir, 'protein_complexes.mat'); || save(outFile, 'data', '-v7'); || fprintf('\\n[OK] wrote %s\\n', outFile);",
    "seed_lines": "",
    "tick_lines": ""
  },
  {
    "file": "extract_transcription_ensemble.m",
    "signature": "function extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite)",
    "save_calls": "save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3');",
    "output_lines": "if ~exist('output_root', 'var') || isempty(output_root) || output_root = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', 'ensembles', 'transcription'); || extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite); || function extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite) || if ~exist(output_root, 'dir') || mkdir(output_root); || fprintf('[transcription_ensemble] output_root: %s\\n', output_root); || seed_dir = fullfile(output_root, sprintf('seed_%03d', seed)); || out_path = fullfile(seed_dir, sprintf('Transcription_%dticks.mat', n_ticks)); || if exist(out_path, 'file') && ~force_overwrite || fprintf('[transcription_ensemble] seed=%d exists, skipping: %s\\n', seed, out_path); || file_info = dir(out_path); || 'path', make_relative_path(out_path, repo_root), ... || fprintf('[transcription_ensemble] skipped write: %s\\n', out_path); || 'path', make_relative_path(out_path, repo_root), ... || save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3'); || fprintf('[transcription_ensemble] saved: %s\\n', out_path); || file_info = dir(out_path); || 'path', make_relative_path(out_path, repo_root), ... || write_manifest(output_root, repo_root, seed_vec, n_ticks, entries); || function write_manifest(output_root, repo_root, seed_vec, n_ticks, entries) || manifest_path = fullfile(output_root, 'MANIFEST.json');",
    "seed_lines": "if ~exist('seed_list', 'var') || isempty(seed_list) || seed_list = 0:49; || extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite); || function extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite) || seed_vec = reshape(double(seed_list), 1, []); || fprintf('[transcription_ensemble] n_ticks=%d, n_seeds=%d\\n', n_ticks, numel(seed_vec)); || 'seed', {}, ... || 'rng_seed', {}, ... || for seed_index = 1:numel(seed_vec) || seed = seed_vec(seed_index); || seed_dir = fullfile(output_root, sprintf('seed_%03d', seed)); || if ~exist(seed_dir, 'dir') || mkdir(seed_dir); || out_path = fullfile(seed_dir, sprintf('Transcription_%dticks.mat', n_ticks)); || fprintf('[transcription_ensemble] seed=%d exists, skipping: %s\\n', seed, out_path); || 'seed', seed, ... || 'rng_seed', uint32(seed), ... || fprintf('\\n[transcription_ensemble] === seed=%d (%d/%d) ===\\n', seed, seed_index, numel(seed_vec)); || seed_tic = tic; || seed_simulation(sim, uint32(seed)); || fprintf('[transcription_ensemble] ERROR seed=%d\\n%s\\n', seed, err_msg); || 'seed', seed, ... || 'rng_seed', uint32(seed), ... || 'elapsed_seconds', toc(seed_tic) ... || 'rng_seed', uint32(seed), ... || 'seed_index', seed_index - 1, ... || 'n_seeds_total', numel(seed_vec), ... || 'seed_list', uint32(seed_vec), ... || 'seed', seed, ... || 'rng_seed', uint32(seed), ... || 'elapsed_seconds', toc(seed_tic) ... || write_manifest(output_root, repo_root, seed_vec, n_ticks, entries); || function seed_simulation(sim, seed) || if isobject(sim) && ismethod(sim, 'applyOptions') && ismethod(sim, 'seedRandStream') || sim.applyOptions('seed', seed); || sim.seedRandStream(); || sim.randStream.seed = seed; || function write_manifest(output_root, repo_root, seed_vec, n_ticks, entries) || missing_seeds = setdiff(seed_vec, [entries.seed]); || all_metadata_seed_match = true; || if entries(i).rng_seed ~= uint32(entries(i).seed) || all_metadata_seed_match = false; || 'seed_range', [min(seed_vec), max(seed_vec)], ... || 'expected_seed_count', numel(seed_vec), ... || 'present_seed_count', numel(entries), ... || 'missing_seeds', missing_seeds, ... || 'all_metadata_seed_match', all_metadata_seed_match, ...",
    "tick_lines": "if ~exist('n_ticks', 'var') || isempty(n_ticks) || n_ticks = 100; || extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite); || function extract_transcription_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite) || fprintf('[transcription_ensemble] n_ticks=%d, n_seeds=%d\\n', n_ticks, numel(seed_vec)); || 'n_ticks', {}, ... || out_path = fullfile(seed_dir, sprintf('Transcription_%dticks.mat', n_ticks)); || 'n_ticks', n_ticks, ... || states_before.(f) = cell(n_ticks, 1); || states_after.(f) = cell(n_ticks, 1); || for t = 1:n_ticks || 'n_ticks', n_ticks, ... || 'n_ticks', n_ticks, ... || 'n_ticks', n_ticks, ... || write_manifest(output_root, repo_root, seed_vec, n_ticks, entries); || % Capture boundary snapshot for tick start before any process-local evolve. || % Capture boundary snapshot for tick end after all process writes are applied. || function write_manifest(output_root, repo_root, seed_vec, n_ticks, entries) || n_ticks_set = unique([entries.n_ticks]); || 'n_ticks_set', n_ticks_set, ... || 'n_ticks_expected', n_ticks ..."
  },
  {
    "file": "extract_translation_ensemble.m",
    "signature": "function extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite)",
    "save_calls": "save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3');",
    "output_lines": "if ~exist('output_root', 'var') || isempty(output_root) || output_root = fullfile(repo_root, 'data', 'm1_sources', 'karr_native', 'ensembles', 'translation'); || extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite); || function extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite) || if ~exist(output_root, 'dir') || mkdir(output_root); || fprintf('[translation_ensemble] output_root: %s\\n', output_root); || seed_dir = fullfile(output_root, sprintf('seed_%03d', seed)); || out_path = fullfile(seed_dir, sprintf('Translation_%dticks.mat', n_ticks)); || if exist(out_path, 'file') && ~force_overwrite || fprintf('[translation_ensemble] seed=%d exists, skipping: %s\\n', seed, out_path); || fprintf('[translation_ensemble] skipped write: %s\\n', out_path); || save(out_path, 'states_before', 'states_after', 'metadata', '-v7.3'); || fprintf('[translation_ensemble] saved: %s\\n', out_path);",
    "seed_lines": "if ~exist('seed_list', 'var') || isempty(seed_list) || seed_list = 0:49; || extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite); || function extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite) || seed_vec = reshape(double(seed_list), 1, []); || fprintf('[translation_ensemble] n_ticks=%d, n_seeds=%d\\n', n_ticks, numel(seed_vec)); || for seed_index = 1:numel(seed_vec) || seed = seed_vec(seed_index); || seed_dir = fullfile(output_root, sprintf('seed_%03d', seed)); || if ~exist(seed_dir, 'dir') || mkdir(seed_dir); || out_path = fullfile(seed_dir, sprintf('Translation_%dticks.mat', n_ticks)); || fprintf('[translation_ensemble] seed=%d exists, skipping: %s\\n', seed, out_path); || fprintf('\\n[translation_ensemble] === seed=%d (%d/%d) ===\\n', seed, seed_index, numel(seed_vec)); || seed_simulation(sim, uint32(seed)); || fprintf('[translation_ensemble] ERROR seed=%d\\n%s\\n', seed, err_msg); || 'rng_seed', uint32(seed), ... || 'seed_index', seed_index - 1, ... || 'n_seeds_total', numel(seed_vec), ... || 'seed_list', uint32(seed_vec), ... || function seed_simulation(sim, seed) || if isobject(sim) && ismethod(sim, 'applyOptions') && ismethod(sim, 'seedRandStream') || sim.applyOptions('seed', seed); || sim.seedRandStream(); || sim.randStream.seed = seed;",
    "tick_lines": "if ~exist('n_ticks', 'var') || isempty(n_ticks) || n_ticks = 100; || extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite); || function extract_translation_ensemble_impl(seed_list, n_ticks, output_root, force_overwrite) || fprintf('[translation_ensemble] n_ticks=%d, n_seeds=%d\\n', n_ticks, numel(seed_vec)); || out_path = fullfile(seed_dir, sprintf('Translation_%dticks.mat', n_ticks)); || states_before.(f) = cell(n_ticks, 1); || states_after.(f) = cell(n_ticks, 1); || for t = 1:n_ticks || 'n_ticks', n_ticks, ... || % Capture boundary snapshot for tick start before any process-local evolve. || % Capture boundary snapshot for tick end after all process writes are applied."
  }
]
