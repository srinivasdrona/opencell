"""Adapters package for L2.event.

Only :mod:`.base` (the interface), :mod:`.fakes` (test doubles), and
:mod:`.ribosome_assembly_smoke` (one optional read-only structural smoke
adapter) live here. See ``scripts/l2_event/adapters/base.py`` docstring for
why no other process adapters exist yet.
"""

from __future__ import annotations
